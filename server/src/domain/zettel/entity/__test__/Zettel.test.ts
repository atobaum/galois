describe("Zettel", () => {
  it("creates", () => {
    // id 있으면 version. uuid, createdAt도 있어야.
    // id 없으면 version 없어야.
    // id 없으면 createaAt = new Date()
  });
  it("toDTO", () => {});
  it("addTag", () => {});
  it("removeTag", () => {});
  it("setRevision", () => {});
});
